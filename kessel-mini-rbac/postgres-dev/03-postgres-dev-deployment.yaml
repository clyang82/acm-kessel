apiVersion: apps/v1
kind: Deployment
metadata:
  name: acm-kessel-postgres
  namespace: acm-kessel-mini-rbac
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: acm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: acm-kessel-postgres
  template:
    metadata:
      labels:
        app: acm-kessel-postgres
        app.kubernetes.io/name: postgres
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: acm
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        command: ["docker-entrypoint.sh", "-c", "config_file=/etc/postgresql/postgresql.conf"]
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: acm-kessel-postgres-pguser-inventoryapi
              key: dbname
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: acm-kessel-postgres-pguser-inventoryapi
              key: user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: acm-kessel-postgres-pguser-inventoryapi
              key: password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        - name: postgres-config
          mountPath: /etc/postgresql
          readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: acm-kessel-postgres-data
      - name: postgres-config
        configMap:
          name: acm-kessel-postgres-config
